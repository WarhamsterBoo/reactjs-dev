export const Engine = {
  firstGeneration: (
    x: number,
    y: number,
    fillngPercentage: number
  ): { isAlive: boolean }[][] => {
    return [];
  },
  nextGeneration(
    creatures: { isAlive: boolean }[][]
  ): { isAlive: boolean }[][] {
    return creatures;
  },
};
